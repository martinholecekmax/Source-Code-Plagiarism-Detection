sessionFactory.getCache().containsEntity(Cat.class, catId); sessionFactory.getCache().evictEntity(Cat.class, catId); sessionFactory.getCache().evictEntityRegion(Cat.class);  sessionFactory.getCache().evictEntityRegions();  sessionFactory.getCache().containsCollection("Cat.kittens", catId); sessionFactory.getCache().evictCollection("Cat.kittens", catId); sessionFactory.getCache().evictCollectionRegion("Cat.kittens"); sessionFactory.getCache().evictCollectionRegions(); // evict all collection data